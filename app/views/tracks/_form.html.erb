<%= form_with(model: track, local: true, url: guest_tracks_path(guest_id: @guest.id), id: 'track_form') do |form| %>
    <%= form.hidden_field :name %>

    <%= form.hidden_field :address %>
<% end %>
